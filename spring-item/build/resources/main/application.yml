spring:
  datasource:
    url: jdbc:mysql://localhost:3306/productList?characterEncoding=UTF-8
    username: root
    password: test
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create